#ok i tried to code when bla takened it

q=NationQuantity*2
while NationQuantity < q:
	{
		NationArray[q-1].NationName = raw_input('Type the name of your nation: ')
		NationArray[q-1].LeaderName = raw_input("What is your name, mighty leader of "+NationArray[q-1].NationName+"?: ")
		q--
	}
#doesn't work
#says invaid syntax and points at equal sign^ like that 

#changed from 
NationArray[0].NationName = raw_input('Type the name of your nation: ')
NationArray[0].LeaderName = raw_input("What is your name, mighty leader of "+NationArray[0].NationName+"?: ")
#want to make it do multiply nations
#works when i do 
NationArray[1].NationName = raw_input('Type the name of your nation: ')
NationArray[1].LeaderName = raw_input("What is your name, mighty leader of "+NationArray[1].NationName+"?: ")


		
# I don't think the program should prompt users to name all the nations, instead we should work on making the program assign random names to the other nations from txt files. - Bla
